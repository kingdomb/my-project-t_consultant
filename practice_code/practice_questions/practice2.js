// 1. Create an object with shorthand properties and a method. Log the object.

const name = "KB";
const age = 30;

// 2. Write a function that takes any number of arguments and returns their product using rest operator.

function multiply(...nums) {
  // your code here
}

// 3. Use spread operator to merge two arrays and two objects. Log the results.

const arr1 = [1, 2];
const arr2 = [3, 4];

const obj1 = { a: 1, b: 2 };
const obj2 = { c: 3, d: 4 };

// 4. Destructure the following object and array with default values:

const settings = { theme: "dark" };
const colors = ["red"];

// 5. Use map and filter on an array [5, 10, 15, 20] to create a new array with doubled numbers greater than 10.

const numbers = [5, 10, 15, 20];

// 6. Write a function that uses destructuring in its parameters to extract name and age from an object.

function printPerson({ name, age }) {
  // your code here
}
